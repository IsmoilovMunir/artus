# Postgres
POSTGRES_DB=aston_admin
POSTGRES_USER=aston
POSTGRES_PASSWORD=change-me
POSTGRES_PORT=5433

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change-me
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_BUCKET=aston-media

# Backend
BACKEND_PORT=8080
CORS_ALLOWED_ORIGINS=http://localhost:5173

# JWT (reserved for upcoming auth work — not yet consumed by the backend)
JWT_SECRET=change-me
JWT_EXPIRATION=3600000

# МойСклад (backend-only — never exposed to the admin frontend)
MOYSKLAD_BASE_URL=https://api.moysklad.ru/api/remap/1.2
MOYSKLAD_API_TOKEN=change-me

# Admin panel
ADMIN_PORT=5173
VITE_API_BASE_URL=http://localhost:8080/api/v1
